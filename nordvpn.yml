#This recipe adds the user to the nordvpn group
setup-nordvpn:
  echo "Adding ${USER} to the nordvpn group.
  sudo usermod -aG nordvpn ${USER}
  echo "Please reboot or log out for changes to take effect."
